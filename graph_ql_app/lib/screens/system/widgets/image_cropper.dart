import 'package:flutter/material.dart';

class ImageCropperWidget extends StatelessWidget {
  const ImageCropperWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
